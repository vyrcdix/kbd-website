# The site itself has no dependencies. This is only for the check tools, which
# want puppeteer-core installed somewhere.
node_modules/
package-lock.json

.netlify/
.DS_Store
Thumbs.db

# Photo masters: full resolution, with camera EXIF. Cloudflare Pages serves the
# repo root, so anything committed here would be published. The web exports
# live in assets/img/; see PHOTOGRAPHY.md.
pics/

# Not part of the site
design-doc/
LAUNCH-CHECKLIST.md
apfel-grotezk-main.zip
.claude/settings.local.json
